import 'package:flutter/material.dart';

class DetallesHab extends StatelessWidget {
  const DetallesHab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
