import 'package:flutter/material.dart';

class Noificaciones extends StatelessWidget {
  const Noificaciones({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
