import 'package:flutter/material.dart';

class NotificacionesPropietario extends StatelessWidget {
  const NotificacionesPropietario({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
